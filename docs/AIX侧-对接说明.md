# 转账加款接口 · AIX 侧对接说明

对应贵方《转账接口对接需求 · AIX 侧》v2.0（2026-08-28）。
AIX 侧已按该文档实现完毕并在本地联调通过，下面是联调所需的全部信息。

其中 **§6 响应签名规则**是贵方文档未定义、由 AIX 侧补齐的部分，请贵方按该节实现响应验签，节内附有可直接对齐的测试向量。

---

## 1. 接口与环境

| 项 | 值 |
|---|---|
| 路径 | `POST /v1/transfer/credit` |
| 生产域名 | `https://aixai.pro` |
| Content-Type | `application/json; charset=utf-8` |
| 认证 | HMAC-SHA256 双向签名，规则完全按贵方文档 §4 |

请求与响应字段、错误码均与贵方文档一致，此处不再重复。

## 2. 加款标的

**加款资产为 WIN，进入用户的「WIN 充值余额」。**

- 该余额可用于 AIX 现有的报单等 WIN 充值资金用途
- 账户标识为请求中的 EVM 地址；AIX 按小写匹配已注册用户
- 地址未注册返回 `2001` 且不加款


## 3. 凭据

| 项 | 值 |
|---|---|
| `partner_id` | `AIX10001` |
| `secret_key`（测试） | 已生成，32 字节随机值，线下交付 |
| `secret_key`（生产） | 已生成，与测试环境不同的另一把，线下交付 |

## 4. 限额

| 项 | 默认值 | 说明 |
|---|---|---|
| 单笔下限 | 10 WIN | 管理后台中可调，低于则 `2003` |
| 单笔上限 | 100,000 WIN | 同上，超出则 `2004` |
| 单日上限 | 1,000,000 WIN | 同上，超出则 `2005` |
| 调用频率 | 10 次/秒 | 超出返回 `5001`（明确未受理，可安全退款） |


> 因贵方文档明确不启用 IP 白名单，`secret_key` 是唯一准入凭证，我方额外按调用方 IP 做了一层基础限速（20 次/秒），防止未认证流量消耗资源。正常调用不会触发。


## 5. 行为确认（对应贵方文档 §2 的三条约定）

三条均已落实：

1. **`success = false` 一定没加款。** 加款事务提交成功后，即使后续步骤（写日志、签名等）出错，也一律返回 `true`。事务本身失败时返回 `5000`（结果不确定），不会返回 `false`，贵方按约定转人工核查而非自动退款。
2. **加款在单个数据库事务内完成**，「查用户 → 加余额 → 落流水」三步同事务，不会出现只加款不记账或只记账不加款。
3. **验签在 nonce 占用之前。** 顺序为：IP 限速 → 报文校验 → `partner_id` → 时间戳 → **验签** → 合作方限速 → **nonce 占用** → 格式与限额 → 加款。

**幂等**：同一 `partner_id + nonce` 只会成功受理一次，重复调用返回 `1004`。此外加款流水以 `partner:{partner_id}:{nonce}` 作为唯一键落库，即使去重记录被清理也不会重复加款。

**nonce 去重实现**：贵方文档建议 Redis `SET NX EX 300`，AIX 侧用 MySQL 唯一约束实现了等价语义（保留窗口同为 300 秒，不小于时间戳窗口）。选择数据库而非 Redis 是为了不引入新的运行时依赖，防重放效果一致。若贵方对实现方式有要求请提出。

**`aix_txn_id` 格式**：`AIXTX` + 8 位日期 + 8 位流水号，例如 `AIXTX2026082800000459`，全局唯一，建议贵方落库保存以便对账。

## 6. 响应签名规则（贵方文档未定义，此处补齐）

贵方文档 §4 的拼接规则建立在「值为原始标量」之上。请求体六个顶层字段全是标量，规则无歧义；但**响应体的 `data` 是嵌套对象，没有可直接拼接的原始值形态**，文档未定义该情况。

**AIX 侧采用的规则：只对顶层标量字段签名，`data` 不参与。**

即参与签名的字段固定为：`success`、`code`、`msg`、`timestamp`、`nonce`。排序、拼接、HMAC-SHA256、小写十六进制输出均与贵方文档 §4 完全一致。

补充两条渲染约定（文档同样未定义）：

- 布尔值 `success` 渲染为小写字面量 `true` / `false`
- 整数 `timestamp` 渲染为纯十进制数字串，无正号、无千分位

### 6.1 请用这两组数据对齐响应验签

与贵方文档 §4.1 同样的密钥：

```
secret_key = "aix_test_secret_key_0123456789ab"
```

**成功响应**（对应 §3 的成功示例）：

```
拼接串：code=0000&msg=success&nonce=c7d21be40a9f&success=true&timestamp=1787875200455
签名：  3e5b79bc5409928fed49d545d828c71b91ac77878973d6fc9c9fe4fe452c4155
```

注意该响应的 `data` 里含 `aix_txn_id`、`address`、`amount`、`credited_at`，但均**不参与**签名计算。

**失败响应**（地址无效）：

```
拼接串：code=2001&msg=address not found&nonce=c7d21be40a9f&success=false&timestamp=1787875200455
签名：  63b1db5c7b1035d41809b4fd994d74bfbb69826a0bcb7ab7520404a68184ceb0
```

> 这组特意选了含空格的 `msg`。按文档 §4 第 3 条「不做 URL 编码」，空格保持原样参与拼接，**不要**转成 `%20` 或 `+`。

以上两组值均由 AIX 侧实现实际计算得出，并已写入自动化测试固定。若贵方算出的结果不同，请在联调前告知，我方配合排查。


## 7. 建议联调顺序

1. **先对签名，两个方向都要对**：
   - 请求方向用贵方文档 §4.1 的向量。AIX 侧已用该向量写入自动化测试，结果一致（拼接串与签名值均完全匹配）。
   - 响应方向用本文档 §6.1 的两组向量。
2. 故意错签 / 重放 nonce / 过期时间戳 → 应分别拿到 `1001` / `1004` / `1003`
3. 未注册地址 → `2001`，贵方侧确认退款链路
4. 已注册地址小额加款 → `0000`，我方在管理端核对 WIN 充值余额与充值流水
5. 限额边界 → `2003` / `2004` / `2005`
6. 生产密钥切换演练（双钥并行）

## 8. AIX 侧已完成的验证

签名对齐与业务分支均已本地跑通：

- 请求方向签名：贵方文档 §4.1 向量的拼接串与签名值完全一致
- 响应方向签名：本文档 §6.1 两组向量已写入自动化测试固定
- 地址大小写：混合大小写地址按原样验签通过，内部按小写正确匹配到用户，加款落库无误
- 错误码实测（本地）：`1001` / `1002` / `1003` / `1004` / `3002` / `3003` / `2001` / `2003` / `2004` 均返回预期业务码与 HTTP 状态
- 成功加款实测：余额、充值流水、`aix_txn_id` 三者一致

---
