# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.pnpm-store
.history
dist
www
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# bundle output
bundle/

# local env (do not commit)
.env
.env.local
# 例外：预览需要 .env.prod 才能拿到链 ID / 合约地址。被忽略的话文件不受版本管理，
# 同步后会丢失，导致注入钱包关闭、首屏永远停在加载圈。
# 里面只有公开的合约地址和公共 RPC，没有任何密钥，因此纳入版本管理。
!.env.prod
